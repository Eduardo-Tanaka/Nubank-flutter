import 'package:flutter/material.dart';

class PagarPage extends StatelessWidget {
  const PagarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("PagarPage"),
    );
  }
}
