import 'package:flutter/material.dart';

class RecargaPage extends StatelessWidget {
  const RecargaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("RecargaPage"),
    );
  }
}
