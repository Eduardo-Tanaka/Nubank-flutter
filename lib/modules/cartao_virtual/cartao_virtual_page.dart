import 'package:flutter/material.dart';

class CartaoVirtualPage extends StatelessWidget {
  const CartaoVirtualPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CARTAOVIRTUAL'),
    );
  }
}
