import 'package:flutter/material.dart';

class TransferirPage extends StatelessWidget {
  const TransferirPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TransferirPage"),
    );
  }
}
