import 'package:flutter/material.dart';

class LimitePage extends StatelessWidget {
  const LimitePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("LimitePage"),
    );
  }
}
